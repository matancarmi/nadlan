# Server-side only (no NEXT_PUBLIC_ prefix): used by next.config.js to proxy
# /api/* requests to the backend, keeping everything same-origin for the
# browser (so the session cookie is first-party, not blocked cross-site).
BACKEND_URL=http://localhost:8000
